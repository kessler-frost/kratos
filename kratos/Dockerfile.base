FROM ghcr.io/astral-sh/uv:debian-slim

# Create working directory
RUN mkdir -p /workdir
WORKDIR /workdir

# Initialize uv project and install common dependencies
RUN uv init
RUN uv add agno cloudpickle openai

# Set the default command to sleep (will be overridden in specific containers)
CMD ["sleep", "infinity"]
